// Here are all the error types that we will be using
export enum ErrorTypes {
    REQUIRED_ERROR = 'required',
    PATTERN_ERROR = 'pattern',
    MIN_ERROR = 'min',
    MAX_ERROR = 'max',
    MIN_LENGTH_ERROR = 'minLength',
    MAX_LENGTH_ERROR = 'maxLength',
}